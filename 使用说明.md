# Forum Application 使用说明

## 🎉 项目已更新为 Podman 优先！

项目现在优先使用 **Podman** 作为容器引擎，同时完全兼容 Docker。

## 🚀 快速开始（三步）

### 第一步：安装 Podman

**方式 1：使用安装脚本（推荐，自动检测是否有 sudo）**
```bash
./INSTALL_PODMAN.sh
```

脚本会自动检测：
- ✅ 如果有 sudo 权限：使用包管理器安装（系统级）
- ✅ 如果没有 sudo 权限：使用静态二进制安装（用户级）

**方式 2：手动安装（需要 sudo）**
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install podman podman-compose

# 验证安装
podman --version
podman-compose --version
```

**方式 3：无 sudo 安装**

如果没有 sudo 权限，查看详细指南：[INSTALL_WITHOUT_SUDO.md](INSTALL_WITHOUT_SUDO.md)

### 第二步：启动应用

```bash
./start.sh
```

脚本会自动检测 Podman 并启动所有服务。

### 第三步：访问应用

打开浏览器访问：
- **前端**: http://localhost:3000
- **后端 API**: http://localhost:8080

## ✨ Podman 的优势

| 特性 | Podman | Docker |
|------|--------|--------|
| 🔒 **安全性** | 无守护进程，rootless | 需要 root 守护进程 |
| ⚡ **启动速度** | 更快 (~3秒) | 较慢 (~4-5秒) |
| 💾 **资源占用** | 更低 | 较高 |
| 🔑 **权限需求** | 无需 root | 需要 docker 组 |
| 🔄 **兼容性** | 100% Docker 兼容 | - |
| 📦 **容器隔离** | 更好 | 一般 |

## 📚 完整文档

- **QUICKSTART.md** - 5分钟快速入门
- **PODMAN_SETUP.md** - Podman 详细配置指南
- **CONTAINER_ENGINE.md** - Podman vs Docker 完整对比
- **PODMAN_MIGRATION.md** - 迁移完成说明
- **README.md** - 完整项目文档

## 🛠️ 常用命令

```bash
# 启动应用
./start.sh

# 停止应用
./stop.sh

# 查看日志
podman-compose logs -f

# 查看运行的容器
podman ps

# 查看使用的引擎
make check-engine

# 重启应用
make restart

# 完全清理（包括数据）
podman-compose down -v
```

## 📋 项目结构

```
forumApp/
├── start.sh                 # 启动脚本（自动检测 Podman/Docker）
├── stop.sh                  # 停止脚本
├── INSTALL_PODMAN.sh        # Podman 安装脚本
├── Makefile                 # 便捷命令
├── docker-compose.yml       # 容器配置（Podman/Docker 通用）
├── 使用说明.md              # 本文件
├── QUICKSTART.md            # 快速入门
├── PODMAN_SETUP.md          # Podman 配置指南
├── CONTAINER_ENGINE.md      # 引擎对比
├── README.md                # 完整文档
├── backend/                 # Go 后端
├── frontend/                # React 前端
└── database/                # 数据库初始化脚本
```

## ❓ 常见问题

### Q: 必须使用 Podman 吗？

A: 不是。项目同时支持 Docker。但**强烈推荐 Podman**，因为：
- 更安全（无守护进程）
- 无需 root 权限
- 性能更好

### Q: 如何从 Docker 切换到 Podman？

A: 非常简单！
```bash
# 1. 停止 Docker 服务
docker compose down

# 2. 安装 Podman
./INSTALL_PODMAN.sh

# 3. 启动（自动使用 Podman）
./start.sh
```

所有数据都会保留！

### Q: Podman 和 Docker 命令一样吗？

A: 完全一样！
```bash
podman ps        = docker ps
podman images    = docker images
podman-compose   = docker-compose
```

### Q: 如果已经安装了 Docker 怎么办？

A: 可以共存。启动脚本会优先使用 Podman，如果没有 Podman 才使用 Docker。

### Q: Podman 需要配置权限吗？

A: **不需要！** 这是 Podman 的最大优势。Docker 需要将用户添加到 docker 组，Podman 完全不需要。

## 🔧 故障排查

### Podman 未安装

```bash
# 错误提示
Error: Neither Podman nor Docker is installed.

# 解决方法
./INSTALL_PODMAN.sh
```

### podman-compose 未找到

```bash
# 安装 podman-compose
sudo apt-get install podman-compose
# 或
pip3 install podman-compose
```

### 端口被占用

```bash
# 检查端口占用
sudo lsof -i :3000
sudo lsof -i :8080

# 或修改 docker-compose.yml 中的端口
```

### 查看详细日志

```bash
# 查看所有日志
podman-compose logs -f

# 查看特定服务
podman-compose logs backend
podman-compose logs database
```

## 📖 学习资源

### 官方文档
- [Podman 官网](https://podman.io/)
- [Podman 文档](https://docs.podman.io/)
- [从 Docker 迁移](https://podman.io/getting-started/migration)

### 项目文档
- `QUICKSTART.md` - 快速入门指南
- `PODMAN_SETUP.md` - Podman 完整配置
- `README.md` - 项目完整文档
- `guidelines/guidv1.0.md` - 架构设计蓝图

## 🎯 下一步

1. **安装 Podman**
   ```bash
   ./INSTALL_PODMAN.sh
   ```

2. **启动应用**
   ```bash
   ./start.sh
   ```

3. **注册账号并开始使用**
   - 访问 http://localhost:3000
   - 点击 "Register" 注册
   - 浏览论坛，创建主题，发表回复

4. **了解更多功能**
   - 查看 README.md 了解完整功能
   - 查看 API 文档学习接口使用

## 💡 提示

- ✅ 使用 `make` 命令更方便：`make start`, `make stop`, `make logs`
- ✅ 启动脚本会自动检测并使用最佳的容器引擎
- ✅ 所有 Docker 命令都可以替换为 Podman
- ✅ Podman 完全不需要 root 权限或特殊组配置
- ✅ 数据在 Podman 和 Docker 之间完全兼容

## 🆘 获取帮助

如果遇到问题：

1. 查看日志：`podman-compose logs -f`
2. 查看文档：`PODMAN_SETUP.md`
3. 检查服务：`podman ps`
4. 重置环境：`podman-compose down -v && ./start.sh`

---

**享受更安全、更快速的容器化体验！** 🚀

创建日期：2025-10-21  
版本：2.0（Podman 优先）

