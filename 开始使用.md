# 🚀 开始使用论坛应用

## ✅ 安装已完成！

Podman 已经成功安装到你的系统，**无需 sudo 权限**！

## 📝 下一步（三个命令）

### 1. 重新加载 Shell 配置

```bash
source ~/.bashrc
```

**为什么需要这一步？** 
- Podman 安装在 `~/.local/bin/`
- 需要重新加载 PATH 才能使用

**或者**：打开一个新的终端窗口

### 2. 验证安装（可选）

```bash
podman --version
podman-compose --version
```

应该看到：
```
podman version 4.9.3
podman-compose version 1.5.0
```

### 3. 启动应用

```bash
./start.sh
```

脚本会自动：
- 检测 Podman
- 下载镜像
- 启动所有服务（数据库、后端、前端）
- 等待服务就绪

### 4. 访问应用

打开浏览器访问：

- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8080

## 🎉 第一次使用

### 1. 注册账号

1. 打开 http://localhost:3000
2. 点击右上角 "Register"
3. 填写：
   - 用户名（至少3个字符）
   - 邮箱
   - 密码（至少6个字符）
4. 点击 "Register" 按钮

### 2. 浏览板块

论坛已经创建了4个默认板块：
- 📢 General Discussion（综合讨论）
- 📣 Announcements（公告）
- 🛠️ Technical Support（技术支持）
- 💬 Off Topic（闲聊）

### 3. 创建主题

1. 点击任意板块进入
2. 点击 "New Thread" 按钮
3. 填写标题和内容
4. 点击 "Create Thread"

### 4. 发表回复

1. 点击任意主题进入
2. 滚动到底部
3. 在回复框输入内容
4. 点击 "Post Reply"

## 🛠️ 常用命令

### 应用管理

```bash
# 启动应用
./start.sh

# 停止应用
./stop.sh

# 查看日志
podman-compose logs -f

# 查看特定服务日志
podman-compose logs backend
podman-compose logs database

# 重启应用
podman-compose restart
```

### 容器管理

```bash
# 查看运行的容器
podman ps

# 查看所有容器（包括停止的）
podman ps -a

# 进入容器
podman exec -it forum-backend bash
podman exec -it forum-database bash

# 查看容器资源使用
podman stats
```

### 数据管理

```bash
# 查看卷
podman volume ls

# 备份数据库（示例）
podman exec forum-database mysqldump -uforumuser -pforumpass123 forumdb > backup.sql

# 完全清理（删除所有数据）
podman-compose down -v
```

## 📚 使用 Makefile（推荐）

项目提供了 Makefile 简化操作：

```bash
# 查看所有可用命令
make help

# 启动服务
make start

# 停止服务
make stop

# 查看日志
make logs

# 检查使用的容器引擎
make check-engine

# 进入数据库
make db-shell
```

## 🔍 检查服务状态

### 检查后端

```bash
curl http://localhost:8080/health
```

应该返回：
```json
{"status":"ok"}
```

### 检查前端

打开浏览器访问 http://localhost:3000，应该看到论坛首页。

### 检查数据库

```bash
podman exec forum-database mysql -uforumuser -pforumpass123 -e "SHOW DATABASES;"
```

## ⚙️ 配置说明

### 环境变量

后端配置在 `backend/.env`（已有默认值）：

```bash
PORT=8080
DB_HOST=database
DB_PORT=3306
DB_USER=forumuser
DB_PASSWORD=forumpass123
DB_NAME=forumdb
JWT_SECRET=your-super-secret-jwt-key
```

### 前端配置

前端配置在 `frontend/.env`（已有默认值）：

```bash
REACT_APP_API_URL=http://localhost:8080/api
```

### 端口配置

在 `docker-compose.yml` 中修改端口映射：

```yaml
services:
  frontend:
    ports:
      - "3000:80"  # 修改3000为其他端口
  backend:
    ports:
      - "8080:8080"  # 修改8080为其他端口
```

## 🐛 故障排查

### 问题 1：podman 命令找不到

```bash
# 解决方法：重新加载配置
source ~/.bashrc

# 或打开新终端
```

### 问题 2：端口被占用

```bash
# 查看端口占用
sudo lsof -i :3000
sudo lsof -i :8080

# 停止占用端口的程序，或修改 docker-compose.yml 中的端口
```

### 问题 3：容器启动失败

```bash
# 查看详细日志
podman-compose logs

# 重新启动
podman-compose down
podman-compose up -d
```

### 问题 4：数据库连接失败

```bash
# 查看数据库日志
podman-compose logs database

# 等待数据库完全启动
podman-compose logs -f database
# 看到 "ready for connections" 就表示成功了
```

### 问题 5：前端无法连接后端

1. 检查后端是否运行：`curl http://localhost:8080/health`
2. 检查浏览器控制台的错误信息
3. 确认 `frontend/.env` 中的 API_URL 正确

## 📖 更多文档

- **使用说明.md** - 中文快速指南
- **无需SUDO安装完成.md** - 安装完成说明
- **INSTALL_WITHOUT_SUDO.md** - 无 sudo 安装详细指南
- **QUICKSTART.md** - 快速入门
- **README.md** - 完整项目文档
- **PODMAN_SETUP.md** - Podman 配置指南

## 💡 开发模式

如果你想修改代码并实时看到效果：

### 1. 只启动数据库

```bash
make dev-db
```

### 2. 本地运行后端

```bash
cd backend
go run main.go
```

### 3. 本地运行前端

```bash
cd frontend
npm install  # 第一次需要
npm start
```

前端会自动打开浏览器并启用热重载。

## 🎯 快速参考

| 操作 | 命令 |
|------|------|
| 启动应用 | `./start.sh` 或 `make start` |
| 停止应用 | `./stop.sh` 或 `make stop` |
| 查看日志 | `make logs` |
| 重启 | `make restart` |
| 进入数据库 | `make db-shell` |
| 清理数据 | `podman-compose down -v` |
| 查看容器 | `podman ps` |
| 检查引擎 | `make check-engine` |

## ✨ 特性说明

### 已实现功能

- ✅ 用户注册、登录
- ✅ JWT 认证
- ✅ 板块管理
- ✅ 主题创建、浏览
- ✅ 帖子回复
- ✅ 帖子编辑、删除
- ✅ 个人资料编辑
- ✅ 浏览计数
- ✅ 回复计数

### 默认数据

- 4个预创建的板块
- 无预创建用户（需要注册）

## 🆘 获取帮助

如果遇到问题：

1. 查看 `故障排查` 部分
2. 查看日志：`podman-compose logs -f`
3. 查看文档：`使用说明.md`, `README.md`
4. 重置环境：`podman-compose down -v && ./start.sh`

---

**祝使用愉快！** 🎉

如有问题，请参考项目文档或查看日志。

创建日期：2025-10-21  
版本：Phase 1 MVP - Podman Edition

