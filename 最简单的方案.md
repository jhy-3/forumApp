# 🎯 最简单的方案 - 本地运行

## ✨ 你说得对！

不需要容器，直接运行更简单！

## 🚀 超级简单的三步

### 第一步：安装 Go 和 Node.js（一条命令）

```bash
./install-dev-tools.sh
```

等待 5-10 分钟，会自动安装 Go 和 Node.js。

### 第二步：配置数据库（5分钟）

**使用 Railway.app（免费，无需安装）：**

1. 打开 https://railway.app
2. 用 GitHub 登录
3. 点 "New Project" → "Provision MySQL"  
4. 复制连接信息
5. 粘贴到 `backend/.env`
6. 在 Railway 的 Query 界面执行 `database/init.sql`

**或跳过**：如果只想看前端 UI，暂时不配置数据库

### 第三步：启动（3秒）

```bash
# 重新加载环境
source ~/.bashrc

# 启动应用
./run-local.sh
```

访问 http://localhost:3000

## 🎊 就是这么简单！

- ✅ 无需 Docker
- ✅ 无需 Podman
- ✅ 无需 sudo
- ✅ 无需管理员

**总耗时：15-20 分钟（首次）**  
**之后启动：3 秒**

## 📋 完整命令（复制粘贴）

```bash
# ==== 首次设置（一次性）====

# 1. 安装开发工具
./install-dev-tools.sh

# 2. 重新加载
source ~/.bashrc

# 3. 验证安装
go version
node --version

# 4. 配置数据库
# 去 railway.app 创建数据库
# 复制连接信息到 backend/.env
# 在 Railway Query 界面执行 database/init.sql 的内容

# ==== 启动应用 ====

# 启动（之后每次只需这一条命令）
./run-local.sh

# 访问
# http://localhost:3000
```

## 🎯 快速测试（跳过数据库）

如果只想快速看一下：

```bash
# 1. 安装工具
./install-dev-tools.sh && source ~/.bashrc

# 2. 只启动前端
cd frontend
npm install
npm start
```

可以看到 UI，但功能会报错（因为没有后端）。

## 📊 对比

| 方案 | 复杂度 | 时间 | 需要 |
|------|--------|------|------|
| **本地运行** | ⭐ | 15分钟 | Go + Node |
| Nix + Podman | ⭐⭐⭐ | 20分钟 | 1GB空间 |
| Docker | ⭐⭐ | 5分钟 | sudo |

## 💡 为什么选择本地运行？

1. **简单** - 不需要理解容器
2. **快速** - 启动只需 3 秒
3. **轻量** - 内存占用少
4. **方便** - 方便调试和开发
5. **学习** - 更容易理解代码

## 🎓 Railway.app 使用指南

### 创建数据库（2分钟）

1. 访问 https://railway.app
2. GitHub 登录
3. New Project
4. Provision MySQL
5. 等待创建完成

### 获取连接信息（1分钟）

1. 点击 MySQL 数据库
2. 切换到 "Connect" 标签
3. 看到连接信息
4. 复制到 backend/.env

### 导入数据（2分钟）

1. 切换到 "Query" 标签
2. 打开 database/init.sql
3. 复制全部内容
4. 粘贴到 Query 框
5. 点击 Run

完成！

## 🆘 需要帮助？

### 安装失败

查看：`本地开发完整指南.md`

### Railway 配置

查看本文 "Railway.app 使用指南" 部分

### 其他问题

查看：`本地运行指南.md`

---

## 🎉 立即开始

```bash
./install-dev-tools.sh
```

15 分钟后，应用就能运行了！

**无需容器，无需 sudo，超级简单！** 🚀

