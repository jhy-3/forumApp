# 🎯 本地开发完整指南（无需容器）

## 适合你的情况！

- ✅ 无需 Docker/Podman
- ✅ 无需 sudo 权限
- ✅ 只测试基本功能
- ✅ 快速开始开发

## 🚀 三步开始

### 步骤 1：安装开发工具（Go + Node.js）

```bash
./install-dev-tools.sh
```

这会安装：
- Go 1.21.5（到 ~/.local/go/）
- Node.js 18（通过 NVM）

时间：约 5-10 分钟

### 步骤 2：配置数据库

**选项 A：使用 Railway.app（推荐，最简单）**

1. 访问 https://railway.app
2. 用 GitHub 账号登录
3. 点击 "New Project"
4. 选择 "Provision MySQL"
5. 复制连接信息：
   ```
   Host: containers-us-west-xxx.railway.app
   Port: 6543  
   User: root
   Password: xxx
   Database: railway
   ```

6. 修改 `backend/.env`：
   ```bash
   DB_HOST=containers-us-west-xxx.railway.app
   DB_PORT=6543
   DB_USER=root
   DB_PASSWORD=你复制的密码
   DB_NAME=railway
   ```

7. 导入数据库结构：
   ```bash
   # Railway 提供的命令行工具，或在他们的 Web 界面执行
   # 复制 database/init.sql 的内容到 Railway 的 Query 界面
   ```

**选项 B：使用本地 MySQL（如果已安装）**

```bash
# 创建数据库
mysql -u root -p -e "CREATE DATABASE forumdb CHARACTER SET utf8mb4;"
mysql -u root -p -e "CREATE USER 'forumuser'@'localhost' IDENTIFIED BY 'forumpass123';"
mysql -u root -p -e "GRANT ALL ON forumdb.* TO 'forumuser'@'localhost';"

# 导入结构
mysql -u forumuser -pforumpass123 forumdb < database/init.sql

# 保持 backend/.env 默认配置即可
```

**选项 C：暂时跳过数据库（仅测试前端）**

可以先只运行前端看 UI：

```bash
cd frontend
npm install
npm start
```

### 步骤 3：启动应用

```bash
# 方式 1：使用脚本（推荐）
./run-local.sh

# 方式 2：手动启动
# 终端 1 - 后端
cd backend && go run main.go

# 终端 2 - 前端
cd frontend && npm start
```

## 📝 详细步骤

### 1. 安装 Go（无需 sudo）

```bash
# 下载 Go
cd /tmp
wget https://go.dev/dl/go1.21.5.linux-amd64.tar.gz

# 解压到用户目录
tar -xzf go1.21.5.linux-amd64.tar.gz
mv go ~/.local/

# 添加到 PATH
echo 'export PATH=$HOME/.local/go/bin:$PATH' >> ~/.bashrc
echo 'export PATH=$HOME/go/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 验证
go version
```

### 2. 安装 Node.js（无需 sudo）

```bash
# 安装 NVM
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 加载 NVM
source ~/.bashrc

# 安装 Node.js
nvm install 18
nvm use 18

# 验证
node --version
npm --version
```

### 3. 配置后端

```bash
cd backend

# 检查 .env 文件
cat .env

# 如果使用在线数据库，修改这些行：
# DB_HOST=你的数据库地址
# DB_PORT=端口
# DB_USER=用户名
# DB_PASSWORD=密码
# DB_NAME=数据库名
```

### 4. 初始化数据库

#### 使用 Railway.app

1. 在 Railway 创建 MySQL 数据库
2. 进入数据库的 "Query" 标签
3. 复制 `database/init.sql` 的全部内容
4. 粘贴并执行

#### 使用本地 MySQL

```bash
mysql -u forumuser -pforumpass123 forumdb < database/init.sql
```

### 5. 启动后端

```bash
cd backend

# 下载 Go 依赖
go mod download

# 运行
go run main.go
```

应该看到：
```
Successfully connected to database
Server starting on port 8080
```

测试：
```bash
curl http://localhost:8080/health
# 应该返回: {"status":"ok"}
```

### 6. 启动前端

**新开一个终端：**

```bash
cd frontend

# 安装依赖（第一次需要）
npm install

# 启动
npm start
```

浏览器会自动打开 http://localhost:3000

## 🎯 快速测试

### 测试后端 API

```bash
# 健康检查
curl http://localhost:8080/health

# 注册用户
curl -X POST http://localhost:8080/api/users/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"password123"}'

# 登录
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"password123"}'

# 获取板块列表
curl http://localhost:8080/api/forums
```

### 测试前端

1. 打开 http://localhost:3000
2. 点击 "Register" 注册账号
3. 登录
4. 浏览板块
5. 创建主题
6. 发表回复

## 🔧 故障排查

### 后端启动失败

```bash
cd backend

# 查看详细错误
go run main.go

# 常见问题：
# 1. 数据库连接失败
#    - 检查 .env 配置
#    - 测试数据库连接：mysql -h host -u user -p

# 2. 端口被占用
#    - 检查：lsof -i :8080
#    - 修改 .env 中的 PORT

# 3. Go 模块下载失败
#    - 检查网络
#    - 设置代理：export GOPROXY=https://goproxy.cn
```

### 前端启动失败

```bash
cd frontend

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 然后启动
npm start
```

### 数据库连接失败

```bash
# 测试数据库连接
mysql -h 数据库地址 -P 端口 -u 用户名 -p

# 或使用 telnet 测试端口
telnet 数据库地址 端口

# 检查 backend/.env 配置是否正确
```

## 💡 开发技巧

### 1. 使用 Air 实现后端热重载

```bash
# 安装 air
go install github.com/cosmtrek/air@latest

# 在 backend 目录运行
cd backend
air
```

代码修改后自动重启！

### 2. 前端已有热重载

React 开发服务器自带热重载，修改代码后自动刷新浏览器。

### 3. 查看日志

```bash
# 后端日志（如果用 run-local.sh）
tail -f logs/backend.log

# 或直接在终端查看
cd backend
go run main.go
```

### 4. 调试

```bash
# 使用 Delve 调试 Go
go install github.com/go-delve/delve/cmd/dlv@latest
cd backend
dlv debug

# 使用浏览器开发工具调试前端
# 打开 Chrome DevTools (F12)
```

## 📊 资源占用

| 服务 | CPU | 内存 |
|------|-----|------|
| Go 后端 | <5% | ~50MB |
| React 前端 | ~10% | ~150MB |
| 总计 | ~15% | ~200MB |

比容器方式轻量很多！

## 🎓 Railway.app 详细指南

### 注册和创建数据库

1. **访问** https://railway.app
2. **点击** "Login" → "Login with GitHub"
3. **授权** Railway 访问你的 GitHub
4. **点击** "New Project"
5. **选择** "Provision MySQL"
6. **等待** 数据库创建完成（~30秒）

### 获取连接信息

1. 点击 MySQL 数据库卡片
2. 切换到 "Connect" 标签
3. 复制显示的连接信息：
   ```
   MYSQLHOST=containers-us-west-xxx.railway.app
   MYSQLPORT=6543
   MYSQLUSER=root
   MYSQLPASSWORD=xxx
   MYSQLDATABASE=railway
   ```

### 导入数据库结构

**方法 1：使用 Query 界面（推荐）**

1. 在 Railway 数据库页面，点击 "Query" 标签
2. 打开 `database/init.sql`
3. 复制全部内容
4. 粘贴到 Query 界面
5. 点击 "Run" 执行

**方法 2：使用命令行**

```bash
# 使用 Railway 提供的连接信息
mysql -h containers-us-west-xxx.railway.app \
      -P 6543 \
      -u root \
      -p \
      railway < database/init.sql
```

### 配置后端连接

修改 `backend/.env`：

```bash
DB_HOST=containers-us-west-xxx.railway.app
DB_PORT=6543
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=railway
```

## ✨ 完整工作流程

```bash
# ==== 一次性设置 ====

# 1. 安装开发工具
./install-dev-tools.sh

# 2. 重新加载环境
source ~/.bashrc

# 3. 配置数据库（Railway.app）
#    - 访问 railway.app
#    - 创建 MySQL
#    - 获取连接信息
#    - 修改 backend/.env
#    - 导入 database/init.sql

# ==== 日常开发 ====

# 启动应用
./run-local.sh

# 或手动启动
# 终端 1
cd backend && go run main.go

# 终端 2  
cd frontend && npm start
```

## 📦 最小化测试

如果只想测试基本功能，甚至可以：

### 1. 只测试前端 UI

```bash
cd frontend
npm install
npm start
```

访问 http://localhost:3000 查看界面（API 调用会失败，但能看到 UI）

### 2. 只测试后端 API

```bash
cd backend
# 配置好数据库后
go run main.go

# 测试 API
curl http://localhost:8080/api/forums
```

## 🎊 总结

**本地运行是最简单的方案！**

**优势：**
- ✅ 无需容器引擎
- ✅ 无需 sudo
- ✅ 启动快（3秒 vs 30秒）
- ✅ 资源少（200MB vs 500MB）
- ✅ 开发体验好

**步骤：**
1. 安装 Go 和 Node.js：`./install-dev-tools.sh`
2. 配置数据库（推荐 Railway.app）
3. 启动：`./run-local.sh`

**时间：**
- 首次设置：15-20 分钟
- 之后启动：3 秒

---

## 🚀 立即开始

```bash
# 1. 安装开发工具
./install-dev-tools.sh

# 2. 重新加载
source ~/.bashrc

# 3. 配置数据库
# 访问 railway.app 创建数据库
# 修改 backend/.env

# 4. 启动
./run-local.sh
```

**就这么简单！无需任何容器！** 🎉

---

更多帮助：
- Railway.app 指南：见本文 "Railway.app 详细指南" 部分
- 故障排查：见本文 "故障排查" 部分
- 完整文档：README.md

