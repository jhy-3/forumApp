# 📖 本地运行指南（无需容器）

## 🎯 概述

直接在本地运行前后端，**完全不需要 Docker/Podman**！

## ✅ 优势

- ✅ 无需容器引擎
- ✅ 无需 sudo 权限  
- ✅ 启动速度快
- ✅ 方便开发调试
- ✅ 资源占用少

## 📋 前置要求

### 必需软件

1. **Go 1.21+** - 运行后端
2. **Node.js 18+** - 运行前端
3. **MariaDB/MySQL** - 数据库（有多种方案）

### 检查已安装的软件

```bash
# 检查 Go
go version

# 检查 Node.js
node --version
npm --version

# 检查 MySQL（可选）
mysql --version
```

## 🚀 快速开始

### 方案 A：使用本地 MariaDB（推荐）

如果系统已经安装了 MariaDB/MySQL：

```bash
# 1. 创建数据库
mysql -u root -p << EOF
CREATE DATABASE forumdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'forumuser'@'localhost' IDENTIFIED BY 'forumpass123';
GRANT ALL PRIVILEGES ON forumdb.* TO 'forumuser'@'localhost';
FLUSH PRIVILEGES;
EOF

# 2. 初始化表结构
mysql -u forumuser -pforumpass123 forumdb < database/init.sql

# 3. 启动应用
./run-local.sh
```

### 方案 B：使用在线数据库（最简单）

使用免费的在线 MySQL 服务：

- [Railway.app](https://railway.app/) - 免费 MySQL
- [PlanetScale](https://planetscale.com/) - 免费 MySQL
- [FreeSQLDatabase](https://www.freesqldatabase.com/) - 免费 MySQL

配置步骤在后面。

### 方案 C：跳过数据库（仅测试前端）

如果只想看前端界面，可以先跳过后端：

```bash
cd frontend
npm install
npm start
```

访问 http://localhost:3000（API 调用会失败，但可以看到 UI）

## 📝 详细步骤

### 步骤 1：配置后端环境

```bash
cd backend

# 复制环境配置
cp .env.example .env

# 编辑配置（如果需要）
nano .env
```

修改 `backend/.env`：

```bash
# 如果使用本地数据库
DB_HOST=localhost
DB_PORT=3306
DB_USER=forumuser
DB_PASSWORD=forumpass123
DB_NAME=forumdb

# 如果使用在线数据库
# DB_HOST=你的数据库地址
# DB_PORT=3306
# DB_USER=你的用户名
# DB_PASSWORD=你的密码
# DB_NAME=你的数据库名
```

### 步骤 2：初始化数据库

#### 方式 1：本地 MariaDB

```bash
# 登录 MySQL
mysql -u root -p

# 执行以下 SQL
CREATE DATABASE forumdb CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'forumuser'@'localhost' IDENTIFIED BY 'forumpass123';
GRANT ALL PRIVILEGES ON forumdb.* TO 'forumuser'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 导入表结构
mysql -u forumuser -pforumpass123 forumdb < database/init.sql
```

#### 方式 2：在线数据库（推荐）

1. 注册 Railway.app 或 PlanetScale
2. 创建 MySQL 数据库
3. 获取连接信息
4. 修改 `backend/.env` 中的数据库配置
5. 导入 `database/init.sql`（通过他们的Web界面或命令行）

### 步骤 3：启动后端

```bash
cd backend

# 下载依赖（第一次需要）
go mod download

# 运行后端
go run main.go
```

应该看到：
```
Successfully connected to database
Server starting on port 8080
```

### 步骤 4：启动前端

**打开新的终端窗口：**

```bash
cd frontend

# 安装依赖（第一次需要）
npm install

# 启动前端
npm start
```

前端会自动打开浏览器访问 http://localhost:3000

## 🎉 完成！

现在你可以：
- 访问 http://localhost:3000 使用应用
- 后端 API 在 http://localhost:8080
- 无需任何容器！

## 🛠️ 使用便捷脚本

我为你创建了自动化脚本。

### 启动应用

```bash
./run-local.sh
```

### 停止应用

按 `Ctrl+C` 停止，或在新终端运行：

```bash
./stop-local.sh
```

## 💾 数据库选项详解

### 选项 1：本地 MariaDB（最快）

**优点：** 速度快，完全控制
**缺点：** 需要安装 MariaDB（可能需要 sudo）

```bash
# 检查是否已安装
mysql --version

# 如果未安装（需要 sudo）
sudo apt-get install mariadb-server
```

### 选项 2：Railway.app（推荐，无需 sudo）

**优点：** 免费，无需本地安装，立即可用
**缺点：** 需要网络连接

**步骤：**

1. 访问 https://railway.app
2. 注册账号（可用 GitHub 登录）
3. 创建新项目
4. 添加 MySQL 数据库
5. 获取连接信息：
   - Host: xxx.railway.app
   - Port: 3306
   - User: root
   - Password: xxx
   - Database: railway

6. 修改 `backend/.env`：
   ```bash
   DB_HOST=xxx.railway.app
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=你的密码
   DB_NAME=railway
   ```

7. 导入数据库：
   ```bash
   mysql -h xxx.railway.app -u root -p railway < database/init.sql
   ```

### 选项 3：PlanetScale（免费，易用）

**步骤：**

1. 访问 https://planetscale.com
2. 注册并创建数据库
3. 获取连接字符串
4. 修改 `backend/.env`
5. 导入 schema

### 选项 4：SQLite（最简单，但需要修改代码）

如果只是测试，可以用 SQLite（无需独立数据库服务）：

```bash
# 需要修改后端代码使用 SQLite driver
# 这个方案较复杂，不推荐初学者
```

## 🔧 常见问题

### Q: Go 未安装怎么办？

```bash
# 下载 Go（无需 sudo）
wget https://go.dev/dl/go1.21.0.linux-amd64.tar.gz
tar -xzf go1.21.0.linux-amd64.tar.gz -C ~/
echo 'export PATH=$HOME/go/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
go version
```

### Q: Node.js 未安装怎么办？

```bash
# 使用 nvm（无需 sudo）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
node --version
```

### Q: 没有数据库怎么办？

使用在线数据库（Railway.app），完全免费且无需安装。

### Q: 后端启动失败？

检查：
```bash
# 查看详细错误
cd backend
go run main.go

# 常见问题：
# 1. 数据库连接失败 - 检查 .env 配置
# 2. 端口被占用 - 修改 PORT 环境变量
# 3. 依赖下载失败 - 检查网络
```

### Q: 前端启动失败？

```bash
cd frontend

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install

# 然后启动
npm start
```

## 📊 性能对比

| 方式 | 启动时间 | 内存占用 | 开发体验 |
|------|----------|----------|----------|
| 容器化 | ~30秒 | ~500MB | 一般 |
| **本地运行** | **~3秒** | **~200MB** | **优秀** ✅ |

## 🎓 开发优势

本地运行的优势：

1. **热重载**
   - Go: 使用 `air` 实现自动重启
   - React: 内置热重载

2. **调试方便**
   - 可以使用 IDE 断点调试
   - 直接查看日志
   - 性能分析更容易

3. **快速迭代**
   - 修改代码立即生效
   - 无需重建镜像

## 🔄 之后每次使用

```bash
# 终端 1 - 后端
cd backend
go run main.go

# 终端 2 - 前端  
cd frontend
npm start
```

或使用脚本：

```bash
./run-local.sh
```

## 🎊 总结

**无需容器的本地运行是最简单的方案！**

**优点：**
- ✅ 无需安装容器引擎
- ✅ 无需 sudo 权限
- ✅ 启动快，资源少
- ✅ 开发体验好

**缺点：**
- ⚠️ 需要安装 Go 和 Node.js
- ⚠️ 需要配置数据库

**推荐用途：**
- 🎯 本地开发
- 🎯 功能测试
- 🎯 学习代码

---

**开始使用：**

```bash
./run-local.sh
```

查看详细步骤：继续阅读下方内容 ↓

